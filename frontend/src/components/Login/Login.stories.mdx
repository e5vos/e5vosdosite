import {Canvas,Meta,Story} from "@storybook/addon-docs";
import Login from ".";

<Meta title="Login/Login" component={Login} />

export const Template = (args) => <Login />;

# Login

<Canvas>
  <Story
    name="Login"
  >
    {Template.bind({})}
  </Story>
</Canvas>
