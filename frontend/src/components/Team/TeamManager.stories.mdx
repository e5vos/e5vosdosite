import { Canvas, Meta, Story } from "@storybook/addon-docs";
import TeamManager from "./TeamManager";

<Meta title="Team/Manage" component={TeamManager} argTypes={{
    team: { control: { type: 'object' } },
}} />

export const Template = (args) => <TeamManager {...args} />;

# TeamManager

<Canvas>
  <Story
    name="TeamManager"
    args={{
      team: {
        name: "Team 1",
        members: [
          {
            role: "admin",
            user: {
              id: "1",
              name: "User 1",
          },
        ],
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
